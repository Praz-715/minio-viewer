<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home - List Buckets</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1 class="my-4">MinIO Buckets</h1>
        <ul class="list-group">
            <% buckets.forEach(bucket => { %>
                <li class="list-group-item">
                    <a href="/bucket/<%= bucket.name %>"><%= bucket.name %></a> - Created on <%= new Date(bucket.creationDate).toLocaleDateString() %>
                </li>
            <% }); %>
        </ul>
    </div>
    <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
